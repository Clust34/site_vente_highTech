{{ form_start(form, {'attr': {'class': 'mt-4 form'}}) }}
<div class="form-email">
	{{ form_row(form.query, {'attr': {'class': 'mt-4'}}) }}

</div>

<h3 class="mt-2 ml-2">Marques :
</h3>
<div class="form-email">
	{{ form_row(form.marques) }}

</div>

<button class="btn btn-primary" type="submit">Rechercher</button>

{{ form_end(form) }}
