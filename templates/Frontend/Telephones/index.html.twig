{% extends 'base.html.twig' %}

{% block title %}
	Fiche produit
	{{ telephone.nom }}
	{{ parent() }}
{% endblock %}

{% block body %}
	<section class="container mt-4">
		<div class="block-detail">
			<div class="block-detail-slider mt-4">
				{% include 'Frontend/Telephones/_sliderDetail.html.twig' %}
			</div>
			<div class="block-detail-content mt-4">
				<h1 class="text-center mb-4">{{ telephone.nom }}</h1>
				<p class="mt-4 mb-4">Prix :
					{{ telephone.prix }}
					€</p>
				<p class="mt-4 mb-4">Marque :
					{{ telephone.marque }}</p>
				<p class="mt-4 mb-4">Description :
					<br>
					{{ telephone.description }}</p>
				<div class="block-button">
					<a href="{{ path('app.contact') }}" class="link">Une question ? Contacter nous.</a>
					<a href="#" class="btn btn-secondary">Ajouter au panier</a>
				</div>
			</div>

		</div>
	</section>
{% endblock %}
